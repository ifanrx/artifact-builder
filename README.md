# artifact-builder

Artifact build automation lives in component subdirectories.

## Components

- [`nginx/README.md`](./nginx/README.md): builds LuaJIT and nginx dynamic module artifacts, then publishes them to S3.
